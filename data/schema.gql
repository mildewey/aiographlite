type Place implements Attributes {
  id: String!
  name: String!
  description: String!
  plane: String!
}

type Character implements Attributes {
  id: String!
  name: String!
  description: String!
  class: String!
}

type Faction implements Attributes {
  id: String!
  name: String!
  description: String!
  alignment: String!
}

type Relationship implements Attributes {
  id: String!
  kind: String!
}
